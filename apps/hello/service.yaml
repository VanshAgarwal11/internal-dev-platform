apiVersion: v1
kind: Service
metadata:
  name: hello
  namespace: dev
  labels:
    app: hello
spec:
  selector:
    app: hello
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
